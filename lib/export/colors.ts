// Excel workbook header colors (bare hex, exceljs argb wants no '#'). Previously imported from lib/presentations/tokens,
// which was deleted along with the PHEV Market Deck; only the two values the workbook uses were kept.
export const COLORS = {
  navy: "0B2545",
  white: "FFFFFF",
} as const;
